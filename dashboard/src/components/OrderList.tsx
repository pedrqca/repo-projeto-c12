import { ClipboardList, Timer } from 'lucide-react'
import type { Order } from '../types/simulation'

export function OrderList({ orders }: { orders: Order[] }) {
  return <section className="panel p-6 sm:p-8"><div className="eyebrow"><ClipboardList size={14} /> Fila de pedidos</div><div className="mt-3 flex items-end justify-between gap-4"><h2 className="text-xl font-bold text-[#f5f0e8]">Carga de trabalho</h2><span className="text-xs font-bold text-[#b9adad]">{orders.length} pedidos</span></div><div className="mt-6 space-y-3">{orders.map((order) => <div key={order.id} className="rounded-lg border border-[#493537] bg-[#2d2021]/65 px-3 py-3 transition-colors duration-200 hover:bg-[#3a2425]"><div className="flex items-center justify-between gap-3"><div className="flex min-w-0 items-center gap-3"><span className="font-mono text-[10px] text-[#927f80]">#{String(order.id).padStart(2, '0')}</span><span className="truncate text-xs font-semibold uppercase tracking-[0.04em] text-[#f0e9df]">{order.dish}</span></div><span className="ml-2 flex shrink-0 items-center gap-1 text-[11px] tabular-nums text-[#c7bdb8]"><Timer size={12} />{order.prepSeconds}s</span></div><div className="load-bar mt-3"><span style={{ width: `${(order.prepSeconds / 8) * 100}%` }} /></div></div>)}</div></section>
}