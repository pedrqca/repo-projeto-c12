import { Area, AreaChart, CartesianGrid, ReferenceLine, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { LineChart } from 'lucide-react'
import type { SimulationResult } from '../types/simulation'

export function SimulationChart({ history }: { history: SimulationResult[] }) {
  const data = history.map((item) => ({ cooks: item.quantidadeCozinheiros, time: item.duracaoSegundos }))
  const minimum = history[0]?.tempoMinimoTeorico ?? 8
  return <section className="panel min-w-0 p-6 sm:p-8"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="eyebrow"><LineChart size={14} /> Curva de desempenho</div><h2 className="mt-3 text-xl font-bold text-[#f5f0e8]">Tempo × cozinheiros</h2></div><div className="flex items-center gap-2 text-xs text-[#b9adad]"><span className="h-2 w-2 rounded-full bg-[#c62828]" /> tempo real <span className="ml-3 h-px w-4 bg-[#d9a441]" /> mínimo teórico</div></div><div className="mt-8 h-72 w-full"><ResponsiveContainer width="100%" height="100%"><AreaChart data={data} margin={{ top: 8, right: 8, left: -20, bottom: 0 }}><defs><linearGradient id="timeFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#c62828" stopOpacity={0.2} /><stop offset="100%" stopColor="#c62828" stopOpacity={0} /></linearGradient></defs><CartesianGrid stroke="#493537" strokeOpacity={0.6} vertical={false} /><XAxis dataKey="cooks" tick={{ fill: '#b9adad', fontSize: 11 }} tickLine={false} axisLine={false} /><YAxis tick={{ fill: '#b9adad', fontSize: 11 }} tickLine={false} axisLine={false} tickFormatter={(value) => `${value}s`} /><Tooltip contentStyle={{ background: '#241a1b', border: '1px solid #6a4848', borderRadius: 8, color: '#f5f0e8' }} labelFormatter={(value) => `${value} cozinheiro(s)`} formatter={(value) => [`${Number(value).toFixed(2)}s`, 'tempo']} /><ReferenceLine y={minimum} stroke="#d9a441" strokeDasharray="4 5" /><Area type="monotone" dataKey="time" stroke="#c62828" strokeWidth={2} fill="url(#timeFill)" dot={{ fill: '#c62828', r: 2.5, strokeWidth: 0 }} activeDot={{ r: 4, fill: '#d9a441' }} /></AreaChart></ResponsiveContainer></div></section>
}