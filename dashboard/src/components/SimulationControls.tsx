import { Minus, Play, Plus, SlidersHorizontal } from 'lucide-react'

interface Props { cooks: number; loading: boolean; onChange: (value: number) => void; onRun: () => void }

export function SimulationControls({ cooks, loading, onChange, onRun }: Props) {
  const setCooks = (value: number) => onChange(Math.min(20, Math.max(1, value)))
  return <section className="panel p-6 sm:p-8"><div className="flex flex-col gap-8"><div><div className="eyebrow"><SlidersHorizontal size={14} /> Configuração</div><h2 className="mt-3 text-2xl font-bold text-[#f5f0e8]">Dimensione a brigada</h2><p className="mt-2 max-w-md text-sm leading-6 text-[#b9adad]">Uma thread por cozinheiro. A mesma fila para todos.</p></div><div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><label htmlFor="cooks" className="mb-2 block text-xs font-bold uppercase tracking-[0.18em] text-[#b9adad]">Cozinheiros ativos</label><div className="flex items-center gap-2"><button className="icon-button" type="button" onClick={() => setCooks(cooks - 1)} disabled={loading || cooks === 1} aria-label="Diminuir cozinheiros" title="Diminuir cozinheiros"><Minus size={17} /></button><output className="flex h-11 min-w-16 items-center justify-center rounded-lg border border-[#6a4848] bg-[#2d2021] px-4 text-xl font-black tabular-nums text-[#f5f0e8]">{cooks}</output><button className="icon-button" type="button" onClick={() => setCooks(cooks + 1)} disabled={loading || cooks === 20} aria-label="Aumentar cozinheiros" title="Aumentar cozinheiros"><Plus size={17} /></button></div></div><button className="primary-button" type="button" onClick={onRun} disabled={loading}><Play size={16} fill="currentColor" />{loading ? 'Processando...' : 'Executar simulação'}</button></div><input id="cooks" className="range-slider mt-1" type="range" min="1" max="20" value={cooks} onChange={(event) => setCooks(Number(event.target.value))} disabled={loading} aria-label="Quantidade de cozinheiros" /><div className="flex justify-between text-[11px] font-bold uppercase tracking-[0.16em] text-[#847678]"><span>1 thread</span><span>20 threads</span></div></div></section>
}